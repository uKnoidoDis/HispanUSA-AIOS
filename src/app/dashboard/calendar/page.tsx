import CalendarView from '@/components/calendar/CalendarView';

export const metadata = {
  title: 'Calendar — HispanUSA AIOS',
};

export default function CalendarPage() {
  return <CalendarView />;
}
